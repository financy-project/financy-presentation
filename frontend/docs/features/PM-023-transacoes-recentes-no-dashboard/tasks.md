# Transações Recentes no Dashboard - PM-023 - Tasks

## Phase 1: Foundation

- [x] F-001: Extend `GET_DASHBOARD` (`src/modules/dashboard/graphql/queries.ts`) to select `recentTransactions { id type description date value category { id title color icon } }`; add `DashboardRecentTransaction` interface and extend `GetDashboardData` per GraphQL/API Blueprint
- [x] F-002: Extend `useGetDashboard()` (`src/modules/dashboard/hooks/use-get-dashboard.ts`): add `recentTransactions: data?.dashboard.recentTransactions ?? []` to `UseGetDashboardResult` and its return object
- [x] F-003: Unit tests for `useGetDashboard`'s new field (`src/modules/dashboard/hooks/__tests__/use-get-dashboard.test.tsx`): returns `recentTransactions` from mocked `data.dashboard.recentTransactions`; returns `recentTransactions: []` before the query resolves
- [x] F-004: Implement `formatDashboardTransactionDate(iso: string): string` and `formatDashboardTransactionValue(cents: number, type: 'EXPENSE' | 'INCOME'): string` (`src/modules/dashboard/utils/format-dashboard-transaction.ts`) — identical implementation to `src/modules/transactions/utils/format-transaction.ts` (UTC-timezone date formatter, no-break-space normalization on value)
- [x] F-005: Unit tests (`src/modules/dashboard/utils/__tests__/format-dashboard-transaction.test.ts`): same cases as `format-transaction.test.ts` — date formats as `DD/MM/YY` in UTC; value formats with `+`/`-` sign per `type`, no `U+00A0`

## Phase 2: Features

- [x] F-006: Extend `useCreateTransaction` (`src/modules/transactions/hooks/use-create-transaction.ts`) with the optional `UseCreateTransactionOptions` parameter per GraphQL/API Blueprint — `refetchQueries: [LIST_TRANSACTIONS, ...(options?.additionalRefetchQueries ?? [])]`
- [x] F-007: Unit tests for the new parameter (`src/modules/transactions/hooks/__tests__/use-create-transaction.test.ts`): existing no-arg test still passes unmodified; new case — called with `{ additionalRefetchQueries: [SOME_QUERY] }` includes it in the mutation's `refetchQueries`
- [x] F-008: Implement `DashboardTransactionRow` (`src/modules/dashboard/components/dashboard-transaction-row.tsx`), props `DashboardTransactionRowProps` per Component Blueprint — local category icon-square (duplicated `ICON_OPTIONS`/`COLOR_OPTIONS`), `Tag` for the category pill, `CircleArrowUp`/`CircleArrowDown` (`size-4`, `text-green-dark`/`text-red-dark`) per `transaction.type`, `formatDashboardTransactionDate`/`formatDashboardTransactionValue` for text
- [x] F-009: Component tests for `DashboardTransactionRow` (`src/modules/dashboard/components/__tests__/dashboard-transaction-row.test.tsx`): renders description/date/category title/formatted value; renders `CircleArrowUp`+`text-green-dark` for `type: 'INCOME'`; renders `CircleArrowDown`+`text-red-dark` for `type: 'EXPENSE'`; renders correctly when `category` is `null`
- [x] F-010: Replace the placeholder in `RecentTransactionsCard` (`src/modules/dashboard/components/recent-transactions-card.tsx`) with real content per Component Blueprint: header (title + "Ver todas" `Button variant="link"` + `Link to="/transactions"` + `ChevronRight`), body (`recentTransactions.map` → `DashboardTransactionRow`), footer ("+ Nova transação" `Button variant="link"` + `Plus`, opens `NewTransactionDialog` via local `open` state), calling `useGetDashboard()` and `useCreateTransaction({ additionalRefetchQueries: [GET_DASHBOARD] })` itself

## Phase 3: Polish

- [x] F-011: Component tests for `RecentTransactionsCard` (`src/modules/dashboard/components/__tests__/recent-transactions-card.test.tsx`, mocking `useGetDashboard`/`useCreateTransaction` same as `transactions-page.test.tsx`'s pattern): shows skeleton rows while `isLoading`; shows `role="alert"` error text; shows `"Nenhuma transação cadastrada ainda."` when `recentTransactions` is `[]`; renders one `DashboardTransactionRow` per item when populated; "Ver todas" links to `/transactions`; clicking "+ Nova transação" opens `NewTransactionDialog`; submitting the dialog calls `createTransaction` and closes it on success
- [x] F-012: Update `dashboard-page.test.tsx`'s existing "renders DashboardHighlights regardless of loading state" test if it now needs to mock `useGetDashboard` differently for the nested `RecentTransactionsCard` call (check whether the existing `vi.mock('@/modules/dashboard/hooks/use-get-dashboard')` + single `mockReturnValue` already covers both call sites, since both `DashboardPage` and `RecentTransactionsCard` call the same mocked hook)
- [x] F-013: Accessibility pass: confirm "Ver todas"/"+ Nova transação" are focusable interactive elements (`Button`/`Link`, not styled `div`s); confirm the error paragraph keeps `role="alert"` — verified in the tests above, no separate manual step
